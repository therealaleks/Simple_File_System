Simple file system mountable through Fuse by Alex Liu

By default, the max number of files is set to be 100, the sfs creates a disk with 2000 blocks, and the max file name length is 20. 
These values can be changed through the macros in either sfs_api.h or sfs_api.c
