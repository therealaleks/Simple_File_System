in my implementation, by default, the max number of files is set to be 100, the sfs creates a disk with 2000 blocks, and the max filename length is 20. 
these can be changed through the macros in either sfs_api.h or sfs_api.c
